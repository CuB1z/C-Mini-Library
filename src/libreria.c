#include "libreria.h"
#include <stdio.h>

int head(int N) {
    printf("Executed head %d\n", N);
    return 0;
}

int tail(int N) {
    printf("Executed tail %d\n", N);
    return 0;
}

int longlines(int N) {
    printf("Executed longlines %d\n", N);
    return 0;
}